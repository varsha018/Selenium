package com.seleniumbasics;

public class DOMStaleElementException {
	
//	HTML DOM is API Interface provided by Browser..It is an Interface to call JavaScript functions to update HTML Document.
//	When a Web Page is loaded, browser creates DOM of the page..

	//	With this Object Model(DOM),JavaScript Gets all the power it needs to create dynamic HTML.
	
	/*
	 * //JavaScript can change all the HTML element in the page. 
	 * 							all HTML attributes in the page
	 * 							all the CSS styles in the page.
	 * 							create new HTML element in the page.
	 * 							remove existing HTML element and attribute.
	 * 
	 * 
	 * DOM>>3 types ...HTML DOM , XML DOM, Core DOM 
	 * 
	 */
	
//	Stale element exception: when you refresh the browser,the browser changes the DOM, and the element you might
//								have searched before refresh might be staled/not present anymore in tham DOM..
//								then we get stale element exception
//
//	
//	
//	DOM---> Provided by Browser(different for every browser(chrome,IE,FireFox)
//	Document Object Model.. The HTML code when you inspect for any element in the page
//	
//	DOM...As per W3C standards..provides CRUD API
//	Add (create),Get(retrive),change(update),delete(Delete)
//	
//	
//	DOM is defined page wise...for fb login separate DOM,for create accnt separate DOM..
//	Every page has separate DOM and it is created at runtime and is different for every browser..
//	
//	
//	
//	
//	
	public static void main(String[] args) {

	}

}

